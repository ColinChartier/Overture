package com.colinchartier.overture.app.playlist.listeners;

import com.colinchartier.overture.app.playlist.Playlist;

public interface OnPlaylistSelectedListener {
    void onPlaylistSelected(Playlist p);
}
